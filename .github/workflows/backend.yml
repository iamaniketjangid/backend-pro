name: Backend CI/CD ⚙️

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: Build & Push Node Backend (latest only)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: "aniuser"
          password: "dckr_pat_-ktgFv9zbZemgourVtBJzSlPtoI"

      - name: Build and Push Docker Image 🐳
        run: |
          cd docker-nodejs-sample
          docker build -t aniuser/node-backend:latest .
          docker push aniuser/node-backend:latest
          
          echo "✅ Successfully pushed latest backend image!"
